// Settings page - currently a placeholder. Future settings (report
// format, dark/light mode, etc.) will read/write via
// browser.storage.local (see the "storage" permission in manifest.json)
// and render their controls here.
